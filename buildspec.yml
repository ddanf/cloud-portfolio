version: 0.1

phases:
  build:
    commands:
      - npm install
      - npm run wpp
artifacts:
  files:
    - index.html
    - favicon.ico
    - Profile.pdf
    - styles/main.css
    - images/*
    - dist/bundle.js
